# Experiment 39 — scope activation depth

For each of the 14 inherited frontier identities, the current delta contract was solved while forcing that identity uncovered. Unlike Experiment 38, width was minimized rather than fixed at 309.

```text
finite-width cases: 0
proven infeasible:  14
timeouts:            0
scope activation gap: ∞
```

Every cover of the 2,508-entry delta necessarily covers every inherited identity under this current candidate/edge contract. Therefore the full and delta feasible-cover families are equal, not merely their width-309 optimal faces:

```text
Feas(C_delta) = Feas(C_full)
```

This is a current-contract result only and does not adjudicate the historical contract.
