# Experiment 53 — residual incidence components

The 142-coordinate residual backbone-incidence graph factors into 125 connected components.

```text
residual coordinates:       142
components:                 125
minimum cover width:        130
residual cover saving:       12
```

Component savings relative to one world per coordinate:

```text
saving 0: 120 components
saving 1:   2 components
saving 2:   1 component
saving 4:   2 components
```

Thus 122 components have width 1, one has width 2, and two have width 3. All component optima were enumerated exactly. Removal and addition component receipts match in aggregate and are preserved separately.

These are incidence/set-cover measurements only; no component is called semantic or causal.
