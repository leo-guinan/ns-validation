# Experiment 47 — geodesic retention and poset test

Among 1,964 ordered local-optimum pairs:

```text
full ambient-geodesic retention: 1888
partial retention:                 76
minimum retention:                 1/18
geodesically convex:              no
```

The component-9 witness has Johnson distance 2, four ambient shortest paths, one retained optimum path, and retention `1/4`.

The retained shortest-path language is not universally representable as linear extensions of a single pairwise precedence poset. Twelve pairs fail that exact test; the receipt preserves a component-31 counterexample with four retained distance-3 paths. Poset events are literal ordered `(remove, add)` pairs from retained exchange edges. This establishes structure beyond one static pairwise precedence poset; whether that structure is state-dependent is measured separately.

Retention and ordering are current-contract graph measurements, not probabilities, semantic adaptivity, or causal claims.
