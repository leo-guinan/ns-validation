# Experiment 60 — exact order and cut structure

The experiment compares the complete pairwise sign structures induced by `p`, `q_tau`, and `q_kappa`, retaining ties rather than collapsing zero inversions into identical rankings.

Equal-`p` groups imply equal `q_tau` in components 11, 12, 21, and 45, but not component 8.

Exact-one feasibility-neighborhood relations:

```text
component 8:  one neighborhood
component 11: one disjoint pair
component 12: one overlapping pair
component 21: one disjoint pair
component 45: one overlapping pair
```

Complete sign tables, exact-one neighborhoods, and pair relations are preserved. These are finite set-cover incidence structures, not semantic or probabilistic claims.
