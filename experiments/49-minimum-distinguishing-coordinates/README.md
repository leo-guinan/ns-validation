# Experiment 49 — minimum distinguishing coordinates

Exact pairwise hitting-set MILPs distinguish every pair-scoped state lying on a source-to-target geodesic. The endpoint-pair identity is supplied as side information.

```text
minimum removal-coordinate dimension: 200
minimum addition-coordinate dimension: 200
raw state-pair comparisons:          2502
removal nonempty masks:              2502
addition nonempty masks:             2502
solver status:                     optimal
```

The selected declaration-coordinate sets and independent replay verification are preserved in the receipt. Every coordinate has a singleton distinguishing witness. These shared literal dimensions are separate from the 13-bit information-theoretic lower bound for indexing 4,104 tagged continuation states.

Scope is pair-scoped: constraints compare states within each ordered endpoint pair. No semantic causal-dimension claim follows.
