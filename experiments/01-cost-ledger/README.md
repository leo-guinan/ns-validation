# Experiment 01 — Cost ledger

Status: prospective.

Goal: build the best evidence-backed lifecycle ledger for the case study. Every quantity must carry provenance (`reported`, `derived`, `estimated`, or `modeled`), source, unit, and status.

Acceptance gate: no derived ratio is calculated until numerator/denominator boundaries and units are explicit.
