# Experiment 44 — optimum exchange geometry

For each component, vertices are exact local minimum hitting sets. An edge joins two optima with symmetric difference 2: one declaration removed and one added.

```text
components:                       302
local exchange graphs connected:  yes
global exchange graph connected:  yes
total local exchange edges:       944
global diameter:                   59
```

Because the global optimum family is the Cartesian product of local families, local connectivity implies global connectivity and the global diameter is the sum of local diameters.

This is width-preserving exchange geometry under the current graph contract. It is not a semantic adaptivity or historical-contract claim.
