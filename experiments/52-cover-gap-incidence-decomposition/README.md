# Experiment 52 — cover-gap incidence decomposition

The minimum world cover decomposes into forced private-coordinate worlds and an exact residual cover. The global literal vocabulary remains a separate quantity: 200 declaration dimensions.

```text
private coordinates (backbone degree 1): 58
singleton-backbone forced worlds:       58
residual coordinates:                  142
residual cover worlds:                 130
minimum world cover:                   188
```

The same exact decomposition holds for removal and addition incidence. The 58 forced worlds cover exactly 58 backbone dimensions, and every forced world has singleton backbone size 1. The full degree histograms, coordinate-level receipts, and canonical forced/residual witness hashes are preserved in the data artifact. The support-degree measurement is kept separate from backbone necessity degree.

This is combinatorial incidence under the finite contract, not a semantic specialization or usage claim.
