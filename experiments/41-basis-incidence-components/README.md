# Experiment 41 — basis incidence components

The 320-class inclusion-minimal constraint basis decomposes into 302 independent connected bipartite optimization components over 472 candidate declarations and 492 incidence edges. The basis classes are irredundant, not mutually independent.

```text
minimum-width sum: 309
component slack sum: 11
exact optimum-family enumeration: complete
exact global optimum count: 1,852,694,344,344,010,752,000,000
exact factorized backbone: 250 declarations
```

The nine multi-class slack-bearing components carry all 11 units of component slack. Singleton-class components may still contain multiple interchangeable local optima. The known `tensorDiff_divergence` ↔ `weak_pressure_poisson` exchange lies in component 31. Componentwise minima and optimum families are exact for the current graph contract. These are not semantic necessity or historical-contract claims.

Witness provenance remains separate. The current stored 309-member witness is hashed, but the exact 309-member baseline swept by Experiment 33 is not preserved. Its identity-level reconciliation therefore remains unavailable; matching widths and the 250/59 cardinalities do not resolve the old cases.
