# Experiment 51 — dimensionality atlas

The 200 globally necessary literal coordinates are distributed across small local worlds.

```text
optional shell:                  200
removal backbone union:          200
addition backbone union:         200
counting lower bound:             67 worlds
exact removal world cover:       188 worlds
exact addition world cover:      188 worlds
```

Each local pair has at most three mandatory coordinates. Forward addition minimum-set families equal reverse removal minimum-set families for all 1,964 ordered-pair reversals.

The receipt preserves per-coordinate necessity degrees, per-pair backbone/support sizes and representation counts, and independently verified minimum-world cover certificates. The observed local-backbone incidence structure raises the exact cover from the counting bound 67 to 188; no single causal mechanism for that gap is asserted.

Under path reversal, `A_{S→T}(u) = R_{T→S}(u)` by the definitions of accumulated additions and removals. Therefore the two distinguishing problems are definitionally isomorphic. The receipt also replay-verifies equality of the generated minimum solution families for all 1,964 ordered-pair reversals.

This is representation incidence under the current finite contract, not usage frequency, semantic causality, or a compression ratio.
